name: arduino_bridge
description: A starting point for Dart libraries or applications.
repository: https://github.com/mowittler/arduino_bridge
version: 1.0.0


environment:
  sdk: ^3.11.1
  flutter: '>=3.41.0'

platforms:
  linux:

# Add regular dependencies here.
dependencies:
  message_pack_dart: ^2.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
